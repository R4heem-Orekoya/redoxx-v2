---
title: Hello World
date: 2026-06-01
---

## Hello World

This is a paragraph of text. It should render with muted foreground color and relaxed line height. Here is some **bold text** and _italic text_ to test inline formatting.

Here is a [link to Google](https://google.com) to test anchor styling.

---

## Code

Inline code looks like this: `dragConstraints` and should have a secondary background.

```ts
const greet = (name: string): string => {
   return `Hello, ${name}!`;
};

console.log(greet("Raheem"));
```

```go
package main

import "fmt"

func main() {
	fmt.Println(isPrime(51))
}

func isPrime(number int) bool {
	if number < 2 {
		return false
	}
	
	for i := 2; i*i <= number; i++ {
		if number % i == 0 {
			return false
		}
	}
	
	return true
}
```

## Blockquote

> The best way to predict the future is to build it. Good software is never finished, only abandoned.

## Lists

Unordered list:

- TypeScript
- Bun
- Elysia.js
- Drizzle ORM

Ordered list:

1. Write the code
2. Ship it
3. Fix the bugs
4. Repeat

## Image

![McLaren P1](https://simonfurlonger.co.uk/wp-content/uploads/2023/12/DP6_0018.webp)

---

That's all for now.
